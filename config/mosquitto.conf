# CyberDeck — Mosquitto MQTT Broker configuration
#
# IMPORTANT: Do NOT set persistence_location here.
# The default /etc/mosquitto/mosquitto.conf already sets it.
# Setting it twice causes Mosquitto to crash-loop.

listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true
