# CyberDeck — dhcpcd static IP for wlan0
# Only used on older Raspberry Pi OS that ships dhcpcd (pre-Bookworm)
# On Trixie/Bookworm, NetworkManager is used instead.

interface wlan0
static ip_address=192.168.4.1/24
nohook wpa_supplicant
